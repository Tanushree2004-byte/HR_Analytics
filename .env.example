# Environment variables (copy to .env for local development)
FLASK_DEBUG=false
API_HOST=0.0.0.0
API_PORT=5000
PORT=5000
CORS_ORIGINS=*
PYTHONPATH=.

# Production (Render)
# CORS_ORIGINS=https://your-app.vercel.app

# Production (Vercel frontend - set in Settings page or inject)
# HR_API_BASE=https://your-render-app.onrender.com/api
